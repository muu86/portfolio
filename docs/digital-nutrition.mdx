export const metadata = {
  company: "디지털 뉴트리션",
  description: "사운드 테라피 스타트업의 1인 백엔드 개발자로 합류",
  techStack: [
    "nodejs",
    "nestjs",
    "typescript",
    "mysql",
    "aws",
    "kubernetes",
    "github-actions",
    "react",
    "webstorm",
    "data-grip",
  ],
  projects: [
    {
      name: "NestJS 도입",
      period: "24.08 - 24.10",
      description: ["기존 Express.js 기반 백엔드를 NestJS 프레임워크로 전환"],
    },
    {
      name: "Terraform 도입 및 AWS 아키텍처 재구성",
      period: "24.08 - 24.10",
      description: [
        "Terraform(IaC) 및 Kubernetes(AWS EKS) 도입",
        "Karpenter로 EKS 클러스터 노드 프로비저닝",
        "RDS, Dynamodb, AWS Lamba, API Gateway 등",
      ],
    },
    {
      name: "CI/CD 파이프라인 자동화",
      period: "24.05 - 24.06",
      description: ["Github Actions 도입", "코드 빌드, 테스트, Helm 배포 자동화"],
    },
    {
      name: "협력 프로젝트 수행",
      period: "24.05 - 24.12",
      description: [
        "살루스케어(비타포트) 앱에서 사운드필 구독권 구매 연동",
        "Payco, DB생명과 제휴 서비스 연동, AI 설문 분석을 위한 Flask 서버 AWS Lambda를 통해 연결",
        "LG 엑스붐 앱에 사운드필 트랙 제휴, AWS Lambda@Edge를 통해 엣지 서버에서 글로벌 고객을 대상으로 인증 처리, 음원 서비스",
      ],
    },
  ],
};

# 디지털 뉴트리션

[**사운드필**: 의학적 근거에 기반한 사운드 테라피 서비스](https://apps.apple.com/kr/app/%EC%82%AC%EC%9A%B4%EB%93%9C%ED%95%84-%EC%88%98%EB%A9%B4-%ED%9C%B4%EC%8B%9D-%EC%A7%91%EC%A4%91%EB%A0%A5/id6450494947)

- 회사의 첫 백엔드 개발자로 합류하여 초기 스타트업이 빠르게 안정적인 서비스를 제공하는데 기여
- 클라우드 인프라(**AWS EKS**)부터 백엔드 서버(**NestJS**) 개발을 전담
- 웹사이트(**React**) 유지보수
- **CI/CD** 파이프라인(Github Workflows) 구축
- 단위 테스트, 통합 테스트 자동화(**Jest**, **Test Container**)

# NextJS 전환

**Express.js** 애플리케이션을 **NestJS**로 전환했습니다.

- **모듈화된 아키텍쳐의 필요성**: NestJS의 **의존성 주입**, **모듈화된 아키텍처**를 기반으로 개발 패턴을 단순화
- **부가 기능 제공**: 프레임워크 차원에서 제공하는 **요청 검증**, **예외 처리**, **자동화 테스트** 등을 활용하여 생산성
  향상
- **타입 안전성**: NextJS과 **ORM** 도입으로 타입 안전성 확보

타 기업과의 협업을 진행하면서, 기존 시스템의 백엔드를 새로 구축해야 하는 큰 부담이 있었습니다. 그럼에도 불구하고,
장기적인 서비스 안정성과 확장성을 고려했을 때, 초기 단계에서 체계를 확립하는 것이 중요하다고 판단했습니다. 전환은
팀원들의 동의를 얻은 후 시작되었으며, 기존 프로젝트와 NestJS 전환 작업을 병행하며 진행했습니다.

개발 기간은 1개월을 목표로 설정했으나, 예상보다 시간이 더 소요되어 **2개월**에 걸쳐 NestJS로의 전환 작업을 성공적으로
완료했고, 그 결과 모듈화된 구조와 테스트 용이성을 갖춘 안정적인 백엔드 시스템을 구축할 수 있었습니다.

## API 문서화와 객체 변환, 검증

NestJS의 **Swagger 모듈**을 사용하여 Typescript **Decorator** 기반으로 API 문서화를 자동화하고 프론트엔드 개발자와의
협업을 더욱 원활하게 수행했습니다.

**class-transformer** 라이브러리를 사용하여 API 요청 객체를 DTO 객체로, DTO 객체를 API 응답 객체로 변환하는 작업을
단순화했습니다. 이를 통해 코드의 유지 보수성과 가독성을 개선할 수 있었습니다. 또한, **class-validator** 라이브러리를
활용해 객체의 유효성을 검증했습니다.

## 보안

NestJS의 **Guard** 컴포넌트를 활용하여 **OAuth**와 **JWT** 기반의 역할(Role) 인증 시스템을 구축했습니다. **사운드필**
앱은 무료와 유료 회원으로 구분되므로, 회원의 권한을 동적으로 처리할 수 있도록 설계했습니다.

```typescript
@Auth(RoleEnum.Free)
@Post('/')
async createRecord(
  @Authorized() principal: Principal,
  @Body() dto: CreateRecordDto) {
  return this.analyticsService.createRecord(dto, principal);
}
```

기존 소스에서 **Google**, **Apple**, **Facebook**, **Naver**, **Kakao**와의 OAuth 연동이 되어 있었고, 백엔드 업데이트
과정에서 **JWT 발급** 및 **토큰 인증**을 NestJS 컴포넌트에 맞게 개선하는 작업을 수행했습니다.

## ORM 도입

Raw Query를 사용하는 기존 소스에서 **타입 안정성**을 강화하기 위해 ORM을 도입했습니다. 처음에는
[**Drizzle**](https://orm.drizzle.team/)을 사용하여 개발을 시작했으나, 단순 쿼리 빌더로서의 한계로 인해 기능적인 제약을
느꼈습니다. 그 결과, [**MicroORM**](https://mikro-orm.io/)을 선택하게 되었으며, Java의 JPA처럼 **변경 추적** 및 **캐시**
기능을 제공하는 MicroORM이 필요로 하는 요구 사항을 충족시켜 주었습니다.

데이터베이스 스키마의 변경으로 **데이터 마이그레이션**이 큰 도전 과제가 되었습니다. Flyway, Liquibase와 같은
마이그레이션 툴을 조사했으나, 이 툴들이 데이터베이스 버전 관리를 주요 용도로 제공하고 있어, 데이터 마이그레이션에는
적합하지 않다는 판단을 내렸습니다. 이에 따라, Node.js를 사용해 별도의 마이그레이션 프로젝트를 개발하여, 변경된 스키마에
맞게 기존 데이터를 변환하고 삽입하는 반자동화 작업을 수행했습니다.

## 구독/결제

결제 이후 스토어에서 구독의 변경, 취소, 갱신을 처리하는 로직이 없었기 때문에, 이를 실시간으로 처리할 수 있는 모듈을
새롭게 개발했습니다.

- **iOS**: **app-store-server-library-node**를 사용하여 처리했습니다.
- **Android**: **Google Cloud Pub/Sub**를 활용했습니다.
- **Web**: **Port One**에서 발급받은 **billing key**를 사용해 정기 결제를 처리했습니다.

또한, **스토어**와 **포트원**에서 **AWS API Gateway** 도메인을 공개하고, **AWS Lambda**를 이용해 실시간 알림을 백엔드
서버로 전송하는 구조로 구현했습니다.

## 테스트 자동화

별도의 QA 팀이 없는 작은 조직에서 테스트 자동화의 중요성을 깊이 인식했습니다. 하지만 시간 제약으로 모든 기능에 대한
테스트 코드를 작성하는 것은 어려운 상황이었습니다.

**구독/결제 파트**만큼은 테스트 코드가 반드시 필요하다고 생각해서 **Jest**, **Test Container** 로 단위 테스트, 통합
테스트를 자동화했습니다.

# AWS 인프라 구축

![aws-architecture](/vpc.png)

## Terraform 도입

- 클라우드 인프라를 쉽게 관리하기 위해서 Terraform을 도입했습니다.

- **명확한 인프라 정의**: 인프라 구성을 코드로 명시하여, 팀원 누구나 전체 아키텍처를 한눈에 파악할 수 있었습니다. 버전
  관리 시스템과의 통합을 통해 변경 내역을 추적하고, 롤백도 손쉽게 수행할 수 있었습니다.

- **팀원 간 협업 강화**: IaC(Infrastructure as Code) 도입으로 인해 모든 인프라 설정이 코드에 기록되어 있으므로,
  개발자들이 인프라 구조를 쉽게 이해하고 공유할 수 있었습니다.

- **모듈화된 재사용성**: Terraform의 Module 기능을 적극 활용하여, 여러 환경(개발, 스테이징, 프로덕션 등)에 동일한 AWS
  리소스를 일관되게 프로비저닝했습니다.

## AWS EKS

### 오토스케일링 문제

기존 백엔드는 Docker Swarm 환경에서 운영되고 있었습니다. 그러나 Docker Swarm은 기본적으로 오토스케일링 기능을 제공하지
않아, 특히 우리 서비스보다 훨씬 높은 트래픽을 처리하는 협력 기업들이 있는 상황에서 **확장성**에 대한 우려가
제기되었습니다. 이에 따라, Docker 컨테이너 환경에서 오토스케일링을 구현하기 위해 ECS Fargate와 EKS를 검토했습니다.

- **ECS Fargate**는 관리형 서버리스 컨테이너 서비스로, 운영이 간편하여 스타트업 환경에 적합해 보였습니다. 그러나 EC2
  기반 인프라에 비해 ECS의 요금이 다소 높게 책정되는 점이 비용 측면에서 부담으로 작용할 수 있었습니다.

- 반면, **EKS**는 클러스터 관리에 필요한 **고정 비용**만으로 운영이 가능해, 장기적으로 볼 때 비용 효율성을 기대할 수
  있어서 선택했습니다.

### Karpenter로 오토스케일링 해결

- Karpenter는 AWS에서 제공하는 클러스터 오토스케일링 솔루션이므로 AWS EKS에서 사용하기 적합하다고 판단했습니다.
- Kubernetes 클러스터 내에서 실행되는 Karpenter Controller Pod를 통해 스케쥴링되지 않은 Pod를 감시하고 필요한
  Node(EC2)를 자동으로 프로비저닝합니다.
- 테스트 결과 2분 ~ 3분 내외로 Node를 프로비저닝하고 5분 안에 Pod를 구동할 수 있었습니다.
- AWS Autoscailing Group은 직접 사용해보진 않았으나, CloudWatch 기반으로 동작하므로 클러스터 내에서 동작하는 Karpenter에
  비해 노드 프로비저닝 속도가 느리다는 걸 확인하여 Karpenter로 결정했습니다.

### 하나의 Kubernetes 클러스터 안에서 여러 환경 배포

- 여러 기업과의 협업 과정에서, 운영 환경에 배포하기 전 각 개발 중인 기능(feature)에 대해 **별도의 API 엔드포인트를
  제공**할 필요가 있었습니다.
- Kubernetes와 Terraform의 **모듈** 기능을 활용하여 동일한 AWS 리소스를 여러 환경에 배포하고, 서로 격리된 환경에서
  테스트할 수 있도록 설정하였습니다. 이를 통해 **비용을 최적화**하면서 **각 환경의 독립성**을 확보했습니다.

### Kubernetes 도입 시 겪은 도전 과제

- **학습 비용**: 팀 내 동료 개발자들이 합류하면서 학습 부담이 발생했습니다.

- **기술 부채**: 퇴사 전, 팀 내에서 EKS 환경을 유지할지 아니면 EC2와 EC2 Auto Scaling 환경으로 전환할지에 대해 논의가
  있었습니다. 저는 EC2 Auto Scaling으로 다시 전환할 것을 제안했으나, 팀원들의 의견을 존중하여 EKS 환경 유지를
  선택했습니다. 상세한 인수인계 문서를 작성하여 이후 팀이 클러스터를 효율적으로 관리할 수 있도록 했습니다. 문서에는
  업데이트 과정과 버전 관리 방법 및 제가 겪었던 오류들에 대한 상세한 설명을 포함시켜, 후속 작업이 원활하게 이루어지도록
  했습니다.

- **버전 관리**: EKS는 구버전 Kubernetes를 사용할 경우 추가 요금이 발생할 수 있어 버전 관리가 중요했습니다. Karpenter
  1.0.0 버전이 정식 릴리즈된 시점에 맞춰, Kubernetes 클러스터와 Karpenter의 버전 업데이트를 성공적으로 진행했습니다.
  버전 업그레이드를 통해 시스템의 성능 안정성과 비용 최적화가 개선되었고, 향후 기술 부채를 줄일 수 있는 기반을
  마련했습니다.

# CI/CD 자동화
